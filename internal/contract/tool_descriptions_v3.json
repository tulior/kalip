{
  "read_ref_B_description": "Read source with snapshot-bound line anchors.\n\nThe @ref R\u003cn\u003e value identifies the observed file version. Copy it\nexactly into splice.ref.\n\nEach source line begins with an anchor such as L38:f1|. Copy the\ncomplete L38:f1 token exactly into splice.at, start, end, or after.\n\nAnchors are supplied by the harness; do not construct or modify them.\nOnly lines shown by this read_ref call can be addressed using its\nanchors. If the file changes or you need anchors for other lines,\ncall read_ref again.",
  "sh_description": "Execute a Bash command in the session workspace.\n\nEach sh call starts a new shell in the workspace working directory.\nShell-local state such as cd, variables, and shell options does not\npersist to later sh calls unless encoded again in the command.\n\nThe command is interpreted by Bash. Nonzero exit status indicates\ncommand failure; output may contain diagnostics.",
  "splice_B_current_description": "Apply atomic edits using anchors copied from read_ref.\n\nFor complete-line or multiline replacement, use start + end + text.\nThe addressed lines are replaced as complete logical lines and the\nharness preserves the boundary to untouched following source.\n\nFor insertion, use after + text.\n\nAll edits use the same snapshot and are validated before mutation.",
  "splice_B_fixed_description": "Apply atomic edits using anchors copied from read_ref.\n\nFor a small replacement within one observed line, use at + old + new.\nold must occur exactly once in that anchored line. Surrounding bytes,\nindentation, comments, and the line boundary are preserved.\n\nFor complete-line or multiline replacement, use start + end + text.\nThe addressed lines are replaced as complete logical lines and the\nharness preserves the boundary to untouched following source.\n\nFor insertion, use after + text.\n\nAll edits use the same snapshot and are validated before mutation.\nSuccessful edits return bounded local bytes read from the committed\nfile. This post-state shows what was written; it does not establish\nsemantic correctness."
}